@model IEnumerable<HockeyProduct>

<!doctype html>

<html lang="en">
<head>
    <meta charset="utf-8">
    <title> Лёд </title>
    <meta name="description" content="The HTML5 Herald">
    <meta name="author" content="SitePoint">
    <link rel="stylesheet" href="~/css/Index.css" />

</head>

<body>
    @foreach(var p in Model)
    {
        <h3>@p.Name</h3>
        <p>@p.Price.ToString()</p>
    }
</body>
</html>